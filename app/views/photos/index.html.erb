<% if @photos.any? %>
  <% @photos.each do |photo| %>
    <h2> <%= photo.title %> </h2>
    <%= link_to "Edit", edit_photo_path(photo) %>
  <% end %>
<% else %>
  No photos yet
<% end %>

<a href='photos/new'>Add a photo</a>